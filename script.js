'use strict';

// Bubbling event
const navbar__list = document.querySelector('.navbar__list');
const nav__bar = document.querySelector('.nav-bar');
const list = document.querySelectorAll('li');

navbar__list.addEventListener('mouseover', function (event) {});

// Sticky Navigation
